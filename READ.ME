# Tabela Hash para Deduplicação de Datasets

## 📝 Introdução
Este projeto implementa uma tabela hash em C++ para resolver eficientemente o problema de deduplicação em datasets, uma etapa crucial no pré-processamento de dados para Ciência de Dados e Machine Learning. A solução oferece complexidade O(n), superando a abordagem tradicional baseada em ordenação (O(n log n)).

## 🏗️ Estrutura do Projeto
.
├── Makefile
├── README.md
├── dataset.csv # Exemplo de dataset de entrada
├── dataset_sem_duplicatas.csv # Resultado após deduplicação
├── main.cpp # Programa principal
├── registro.hpp # Definição da classe Registro
└── tabela_hash.hpp # Implementação da Tabela Hash


## 🛠️ Como Usar

### Pré-requisitos
- Compilador C++11 (g++ ou clang++)
- Make

### Compilação
```bash
make